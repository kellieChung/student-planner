# Progress log

Read first; update before ending a session (changes, decisions, TODOs). Scratchpad for continuity, not documentation (that's `CLAUDE.md` + code comments). Older detail lives in `git log`.

## Active TODOs

- **Alpha audit (2026-09-25), deploy together:** pushing removes `/api/extension/auth/start` + `/exchange` and hashes extension tokens, so every installed 0.1.0 extension stops working. Push, then immediately send testers extension **v0.2.0**. Manual extension test still owed: set `config.js` to localhost, load unpacked, Sign in → Connect page → popup shows signed in; open `/extension-callback?state=<64 hex>` in a normal tab and confirm the extension rejects it; sync, cancel, reopen popup mid-sync; revert `config.js`.
- `TERMS_VERSION` bumped to 2026-09-25 (privacy policy now discloses assignment text → Anthropic): everyone re-accepts once; extension tokens 401 until they do. Revert = one line in `lib/legal.ts`.
- Check Vercel function max duration (Settings → Functions): announcement checks can take ~120s (90s extraction budget + dup check). No route exports `maxDuration` (plan unknown; 60 would lower a 300s fluid-compute limit).
- Confirm `YOUTUBE_DATA_API_KEY` is set in Vercel (playlist import 500s without it).
- Contact email is temporarily kelliecpiano@gmail.com (`CONTACT_EMAIL`, `lib/legal.ts`); switch to an official domain address.
- Not click-tested: Canvas due-date freeze fix, Canvas tombstones, sync/restore, extension relay, failed-load banner, Pomodoro chime, Music onError/skip, Rundown close guard.
- Dev accounts can still full-replace XP via `PATCH /api/gamification` (dev panel only) — it bypasses the award ledger; don't script it.
- Planner Pause is hidden (`PAUSE_AVAILABLE` in `DetectionTriggerControls`): with per-check cap = batch size, a check is one batch. Resumes are capped at 3 per run (`detection_pass_resumed` events).

- **Google sign-in (2026-09-24):** production moved to `lodestarplanner.vercel.app`; login failed with `redirect_uri_mismatch`. In Google Cloud Console add redirect URI `https://lodestarplanner.vercel.app/api/auth/callback/google` and JS origin `https://lodestarplanner.vercel.app` (keep localhost). Check Vercel has no stale `AUTH_URL`/`NEXTAUTH_URL`. Not yet confirmed working.
- **Re-send the Chrome extension** to alpha testers (old builds call the dead URL). When it's published, set `EXTENSION_STORE_URL` in `lib/extensionInstall.ts`.
- **Temporary logo:** replace `public/brand/lodestar-{logo,mark}-temp.png`, `app/icon.png`, `app/apple-icon.png`, `canvas-extension/icons/*.png`.
- **Never clicked through signed-in (only tsc/lint/mock pages):** new date/time/select/colour/tooltip/switch/slider controls in every real form (add/edit task, recurrence, Rundown, Courses, Music, Taskbar), the announcement analyzer (quota, pause/resume, regenerate, per-check cap, `/dev`), the Rundown window, onboarding tour (incl. `/dev/onboarding`), Star Chart (earn, chart, completion), email/password sign-up/sign-in/consent gate/Delete Account, account settings, real extension flows (sign-in, tab detect, sync/cancel, restore) and Safari/Firefox/real-phone checks for the landing hero.
- Set `DEV_ACCOUNT_EMAILS` in Vercel (only in local `.env` so far) and confirm `ANTHROPIC_API_KEY` there.
- Get `/terms` and `/privacy` reviewed.
- Auth follow-ups: email verification, password reset, rate-limit login/sign-up, sign out other devices on password change, email change on `/settings/account`. React clears the sign-up form (incl. consent boxes) after a failed action.
- Add the remaining 73 constellations to `lib/constellations.ts`; Nebula cosmetics not started; delete retired town/World code once sure.
- Recurring tasks: never wired "clone the first occurrence's AI estimate" (a long series costs one estimate per occurrence); monthly materialization and paused series only unit/toggle-checked.
- Canvas: sync only prunes whole courses (not single assignments); a hidden course can vanish if it drops off Canvas's list; a task overridden to a custom course name goes stale if that course is renamed.
- Inline emoji remain in some planner content (e.g. estimating line, task badges); chrome/taskbar/windows were converted.
- Server-side day math still uses the server clock in a few places (see Weekly grid, below).

## Deployment & environment
- Vercel, repo `kellieChung/student-planner`, auto-deploys on push to `main`; production `https://lodestarplanner.vercel.app`. Env vars are set in Vercel (not the gitignored `.env`) and apply on next deploy. **Production and local dev share one Postgres DB** — local testing touches real rows.
- `canvas-extension/config.js` `APP_ORIGIN` is the extension's only backend URL (no UI for it; no build step). Local extension dev: edit it to `http://localhost:3000`, reload, revert before committing.

## Auth & accounts
- JWT sessions (`auth.ts`; Credentials can't use DB sessions); the `Session` table is unused. A JWT can outlive a deleted user, so `/login` and `/` verify the user row exists.
- Sign-up is a server action (`app/login/actions.ts`); emails trimmed+lowercased. Passwords: `lib/password.ts` (scrypt, salted). **Policy (user decision): 8–128 chars + uppercase + special char** (`lib/passwordRules.ts`, shared by the form's live checklist and server checks in `signUp`/`changePassword`); sign-in unchecked so old passwords work. No confirm-password field; show/hide toggle instead.
- Sign-up never attaches a password to an existing (e.g. Google) email, and `allowDangerousEmailAccountLinking` is off (account-takeover risk without verification); consequence: `OAuthAccountNotLinked` for someone whose email was password-registered first.
- Consent: `User.termsAcceptedAt/termsVersion/ageConfirmedAt`; Google and legacy users go to `/accept-terms`. Bump `TERMS_VERSION` (`lib/legal.ts`) to re-prompt everyone. 13+ (COPPA) gate. `/terms` and `/privacy` are templates (operator Kellie Chung, Nevada law); keep the privacy page's data-flow list in sync with new third parties.
- Delete Account (`DELETE /api/account`, cascades) lives in `/settings/account`, which also edits name and sets/changes password. Email is read-only until verification exists.
- Login UI: segmented Sign in/Create account (solid gold only on real primary buttons), branded checkboxes, explicit `<label for>`, static stars + faint Orion/Big Dipper, and a card Cassiopeia (`AuthProgress.tsx`) that lights as required steps complete. `--field-border` token exists because `--border` failed WCAG 3:1 for form controls. Put `auth-page` on any new auth screen's `<main>` for the shared focus ring.

## Canvas & extension
- Extension (`canvas-extension/`) reads Canvas with the student's own session and posts to `app/api/canvas/sync` via a `Bearer` `ExtensionSession` token (Canvas gives students no tokens). **Pairing:** the extension generates a random state and opens `/extension-login?state=`; `/extension-callback` is a Connect confirmation whose server action creates the session (token stored as sha256, `lib/extensionAuth.ts`) and returns the plaintext only to that page, which `postMessage`s it; `site-bridge.js` (content script on the app origin) relays it and `background.js` accepts it only for its own stored state. No pollable exchange endpoint. Sign out revokes (`DELETE /api/extension/auth/session`); password change deletes all sessions. Sync is per-course fault-tolerant (`failedCourseIds` are not pruned), sends only fields ingest reads, fetches announcements with explicit `start_date`/`end_date` (Canvas defaults end to start+28d), and the popup renders from `canvasSyncProgress` storage. Follows `Link` header pagination. Popup follows the app's Night/Day theme.
- Popup: Night/Day tokens, bundled Manrope/Spectral, inline-SVG icons (no emoji), single `state`→`render()`, real brand mark icons. Sync button is replaced by progress+Cancel while running; status chips are flat non-interactive pills; restore-a-course sits in a closed Troubleshooting `<details>`; header constellation lights per step (signed in, Canvas connected, synced). Canvas URL is auto-detected from open tabs (hostname heuristic, then `IS_CANVAS_PAGE` reply from `content.js`), else `[name].instructure.com` or a pasted full host; connect verifies `/api/v1/users/self`. No school list, no debug URL override (both declined).
- Deleting a course hard-deletes it and writes a `DeletedCanvasCourse` tombstone (one transaction; skipped for custom courses); `lib/canvasIngest.ts` skips tombstoned courses; only `/api/canvas/restore-course` removes one. Sync also filters excluded courses client-side (`/api/canvas/excluded-courses`, fails open). Pruning courses missing from a sync writes no tombstone by design.
- Onboarding tour step explains the extension (load-unpacked instructions until `EXTENSION_STORE_URL` is set).

## AI pipeline (Claude Haiku primary, Ollama fallback)
- Every AI call uses Haiku when `ANTHROPIC_API_KEY` is set, else local Ollama; each has a timeout and deterministic fallback. Shared `lib/ai/anthropicClient.ts` (`logAnthropicUsage`), `lib/ollamaConfig.ts` (`NUM_CTX=8192`). **Any Canvas HTML must go through `lib/htmlText.ts` (`stripHtml`/`truncateText`) before a prompt** (unbounded HTML once blew the context window). XP (`task-xp`) is time-based, no model call.
- Cost control (target ≤ ~$0.05/user/week): `Announcement.aiAnalyzedHash` skips unchanged text (set only on real extraction with no degraded dup check; legacy reviewed announcements are backfilled without a call); extraction batches 10/call, dup check is one call per batch (exact/containment pre-pass, containment capped at medium confidence); assignment scoring batches 20/call; `/api/task-planning` reuses matching stored estimates, only scores the user's own task ids, caps 200 analyses/24h, and never stores fallback results. No prompt caching/Batches (prompts under Haiku's cache minimum; Batches is async).
- The duplicate checker is biased to false positives in code (`uncertainDuplicateResult`); `checkStatus` `"checked"` vs `"degraded"` (only degraded shows grey "unavailable"); matches resolve by 1-based number, never a model-typed id.
- Extraction failures retry by bisection; the `RULES` block goes only in the system message.
- Quota (`lib/aiRateLimit.ts`): 2 checks/week + bonus credits (spent weekly-first), all derived from `AiTaskEvent` rows (`detection_pass_triggered/paused`, `detection_credit_granted`); credit-paid runs don't count against the week. `DEV_ACCOUNT_EMAILS` only unlocks `/dev` and `/api/dev/*` (no rate-limit bypass). Per-check cap `MAX_ANNOUNCEMENTS_PER_CHECK = 10` (`lib/analysisLimits.ts`, newest first; >10 explicit selection is a 400); resume is restricted to the run's charged ids. Pause/resume is cooperative (one stream; pause event checked before each batch; resume free within 24h). **Regenerate** re-runs the range (1 check) and replaces a same-key pending candidate (a re-worded task can resurface a declined one). `/dev` dashboard: accounts/credits, analyzer reset, tour/map launchers; all `/dev/*` go through `requireDevUser` (404 otherwise).

## Rundown (auto task creation; spec `AutoTaskCreation.md`)
- Opens as a floating window (`RundownWindow`, portalled into LaptopFrame's floating layer) when something is new since `PlannerSettings.lastRundownViewedAt`. The AI pass is manual only ("Check for announcements"); opening never calls a model.
- `AnnouncementSuggestionReview.status`: pending/accepted/rejected/maybe (a row per detected candidate, `taskSnapshot` JSON); Maybe items live in Still Deciding. `finalizeCandidates` is the single writer and applies auto-accept (high-confidence non-dup → real `CustomTask`; high-confidence dup → suppressed; `lib/rundownAutoAccept.ts`). Already-decided candidates never re-enter the pipeline, in any mode. `AiTaskEvent` also logs accept/dismiss/delete signals; `CustomTask.aiTagDismissedAt` drives the AI badge.
- Compact candidate card with opt-in evidence. Evidence highlight fell back to a whitespace/emoji/quote-insensitive match (model quotes come from `extractActionableHtml`); index maps must be per UTF-16 unit.
- Gotcha: `globals.css` remaps any class containing `bg-red-50`/`border-red-` under `.theme-surface`, so `hover:bg-red-500/10` is always on; use `--status-overdue-text`.

## Planner, scoring, scheduling
- Priority (`lib/prioritization.ts`, spec `prioritizationModule.md`): urgency dominates; importance/difficulty/consequence/frog only break ties. A future custom start date forces score 0 (gate, not a weight; overdue wins over a stale `startAt`); explicit Focus is ungated. Procrastination index per type shifts effective due dates earlier (capped). Time estimates are a type→minutes lookup (`estimateMinutesByType`); auto-estimation covers tasks due within 3 weeks, max 60.
- `Assignment.due` is plain `"YYYY-MM-DD"`; `dueAt`/`dueFraction` resolve in the browser's timezone. **Never pass a server-built `Date` day-anchor to the client** (this once shifted every task a day) and never use `new Date("YYYY-MM-DD")`/`toISOString()` for a calendar day — use `parseLocalDate`/`toDateKey`/`getTodayString`/`daysBetween` (`lib/utils.ts`). The announcements route takes window instants + `tzOffset` from the browser; `recurring-tasks/[id]` takes the browser's `today` (`resolveClientToday`); `task-customizations` stores `completedAt`/`startAt` as UTC-midnight date keys (round-trips). Week is Sunday-start (`getStartOfWeek`).
- State is per `(userId, taskId)` in `TaskCustomization`/`CustomTask`/`TaskPlanningEstimate`/`ProcrastinationRecord`. `persistCustomization`'s PATCH is a full-row replace: **spread the current customization before overriding**. Overrides: `nameOverride/typeOverride/dueAtOverride/course`; `typeOverride` is the only storage for a custom task's type; `dueAtOverride` is one instant (date-only overrides synthesize `endOfDayInstant`). `completedAt`/`startAt` are `"YYYY-MM-DD"`. Custom start dates lapse at read time (`hasCustomStartDatePassed`), never via a PATCH. Task status is derived 3-way (`lib/taskStatus.ts`).
- Weekly grid: chronological (due → time → course), absolutely positioned via `packColumnOffsets` (skyline packer; not CSS Grid rows); completed cards shrink/dim but never move; small gaps are expected (greedy first-fit).
- Card label `COURSE - TYPE - DAY - NAME` (`lib/taskLabel.ts`); type code HW/R/EXAM/TODO (TODO = personal `Course.isCustom`); the 14-value `AssignmentType` still drives scoring. Course abbreviation/colour are user-editable `CanvasCourse` columns with hash defaults (`lib/courseColor.ts`).
- Recurring tasks: `RecurringTask` template; occurrences are real `CustomTask` rows (deterministic ids `custom-r<id>-<due>`) materialized client-side (`expandOccurrences`, 8-week horizon, on mount). Deleting one occurrence is a `TaskCustomization` tombstone (hard delete would resurrect). Scope prompt "this / this and following" (`applyFromDate`, `deleteFrom`); refetch (`refreshCustomTasks`) after series changes.
- Localstorage-only by design: pomodoro state/active task, theme, music volume, window layout.

## UI system
- Themes: `data-theme` `dark` = **Night** (navy/gold), `light` = **Day** (cream/ink/deep gold `#8a6418`); tokens in `globals.css` (`--accent`, `--accent-contrast`, `--field-border`, …); a slate→token remap under `.theme-surface`/`.planner-shell` carries planner components. Fonts: Manrope body, Spectral headings.
- `components/ui/` (Radix; styled `.lodestar-*`): `Select` (maps `""` to a sentinel), `Checkbox`, `Switch`, `Slider`, `NumberField`, `TimeField`, `ColorField`, `ConfirmDialog` (replaces `confirm()`), `Tooltip` (replaces `title=`); plus `DatePicker` (react-day-picker 10): **closed state is a typeable `mm/dd/yyyy` field + calendar icon; the themed calendar pops open only on click (user decision 2026-09-25: no dropdown-looking box, no always-visible calendar)**; native picker on `(pointer: coarse)`. `Select` ignores Radix's spurious `""` change (it used to blank the edit modal's Status and reset progress on save). Popups portal to `<body>` (`z-[100]`, tokens only); **don't add `focus:outline-none` to their `className`**. Icon-only buttons need an `aria-label`. Left native on purpose: auth-form checkboxes, retired World/MapEditor/Bard controls.
- Time: `TimeField` is typed text + AM/PM buttons (`parseTypedTime` in `lib/timeOfDay.ts`); stored `"HH:MM"`. Colours: 7 hues × light/medium/deep + hex box; `readableTextColor` picks badge text (navy/white) by WCAG contrast.
- Landing: `ScrollHero` writes `--p` (0–1) from one scroll listener; sticky stage in a `2.6×100svh` header (**no `overflow` on the stage's ancestors**); timeline constants in `heroTimeline.ts` (sky draws 0.12–0.85, headline 0.6–0.85, hold to 1); 3 parallax layers; no-JS/reduced-motion pin `--p:1`. `DemoPlanner` ("Try it") is client-only, fed server-built labels (`demoTasks.ts`; `lib/taskLabel` no longer pulls in the Anthropic SDK since the pure helpers moved to `lib/assignmentType.ts`, so this could be simplified); styled with `--ls-*` tokens, not `.theme-surface`. Spec `HomepageSpec.md`.
- Onboarding: `TourSpotlight` engine with `data-tour` anchors (add one alongside any new step; a missing/hidden target centres the card); steps in `components/starchart/Onboarding.tsx` (memoized so `before()` doesn't re-run); `/dev/onboarding` previews it on the real planner. Replay from settings.
- Shell: full-bleed `h-dvh` LaptopFrame; the Rundown, The Watch (Pomodoro) and Comms (music) are draggable windows (`components/os/Window.tsx`, `WindowManagerContext` clamps to the viewport; resize grip's hit area must not be clipped by the rounded corner; Music stays mounted while open so audio continues; Pomodoro force-writes a paused snapshot before close).

## Gamification: Star Chart (spec `gamificationSystem.md`)
- Completing a task earns Starlight = the XP award via `POST /api/gamification {action:"award", taskId}`: ownership-checked, amount from `lib/xp.ts`, dedup + XP + Starlight increments in one transaction (no client-set totals; star-chart `earn` and `/api/task-xp` are gone). Spend it to chart stars in real IAU constellations (`lib/constellations.ts`, 15 of 88; unlock by lifetime Starlight; price 25+5×index). `StarChart` + `ChartedStar` tables; Starlight changes only server-side by increment/decrement (award above; `app/api/star-chart` `chart` in one transaction with a conditional decrement; P2002 → 409). One `StarChartProvider` under `LaptopFrame`. Ship's Log ↔ Star Chart uses pull-back/push-in zoom classes applied only while animating (a lingering transform re-anchors fixed modals).
- Retired but kept (header "RETIRED"): town/World, Nano, `TownState`, `WorldLayout`, `/api/town-state`, `lib/townGrowth.ts`, sprites, `/dev` map editor. No streak mechanic — don't reintroduce without asking. The old `GamificationState` table still backs XP.
- Naming: True North (home), Ship's Log (planner), Polaris (top task), Star Chart, Starlight, The Watch, Comms.

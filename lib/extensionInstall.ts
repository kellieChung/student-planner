// Where students get the Lodestar Canvas Chrome extension. Alpha testers are
// sent the extension files directly and load them unpacked, so this is null.
// Once the extension is published, set it to the Chrome Web Store listing URL:
// the onboarding step then shows a "Get the extension" button instead of the
// load-unpacked instructions.
export const EXTENSION_STORE_URL: string | null = null;
